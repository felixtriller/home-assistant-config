version: '3'
services:
  homeassistant:
    image: homeassistant/raspberrypi3-homeassistant:0.76.2
    restart: always
    network_mode: host
    volumes:
      - .:/config
      - /etc/localtime:/etc/localtime:ro
  deconz:
    image: marthoc/deconz
    restart: always
    network_mode: host
    volumes:
      - ../deconz:/root/.local/share/dresden-elektronik/deCONZ
    devices:
      - /dev/ttyUSB0
    environment:
      - DECONZ_WEB_PORT=8080
      - DECONZ_WS_PORT=8443
